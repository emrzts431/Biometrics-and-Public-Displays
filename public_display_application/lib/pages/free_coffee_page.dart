import 'package:flutter/material.dart';

class FreeCoffeePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Column(
      children: [
        Text('FREE COFFEE'),
        Text('TOUCH THE SCREEN RIGHT NOW!!!!'),
      ],
    );
  }
}
